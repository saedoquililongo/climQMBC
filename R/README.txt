To install the climQMBC package download the .tar.gz file and run the following line in R:

	install.packages("<dir>\\climQMBC_0.1.0.tar.gz",repos=NULL,type="source")

where <dir> is the directory where the .tar.gz file is located.